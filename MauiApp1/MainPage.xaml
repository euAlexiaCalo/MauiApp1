<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             Title="Tela de cadastro">
    <!--ctrl + K + C = comenta e pra desfazer ctrl + K + U-->
    <ScrollView Orientation="Vertical">
        <Grid>
            <!--Definição das linhas do Grid-->
            <Grid.RowDefinitions>
                <RowDefinition Height="250"/>
                <RowDefinition Height="*"/>
                <!-- * - essa linha vai ocupar uma parte do espaço disponível restante na tela -->
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <!--Agrupar elementos que precisam de espaçamento consistente.-->
            <!--Linha 1: imagem e título-->
            <StackLayout Grid.Row="0" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center">
                <Image Source="dotnet_bot.png" Aspect="AspectFit" MaximumHeightRequest="200" HorizontalOptions="Center"/>
                <Label Text="Faça o seu cadastro" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
            </StackLayout>

            <!--Linha 2: campos do formulário-->
        <!-- Entrada de dados:
                Entry → campo de texto de linha única

                Editor → campo de texto de múltiplas linhas

                DatePicker → seletor de data

                TimePicker → seletor de hora

                Picker → lista suspensa para selecionar um item

                CheckBox → caixa de seleção (verdadeiro/falso)

                Switch → botão liga/desliga

                RadioButton → opção única dentro de um grupo

            Controles de Apresentação:

                Label: Exibe texto.

                Image: Exibe uma imagem.

                Button: Botão clicável.

                ImageButton: Botão clicável com imagem.

                ProgressBar: Exibe o progresso de uma operação.

                ActivityIndicator: Exibe um indicador de atividade (carregando).

                IndicatorView: Exibe indicadores de página (como pontos de navegação).

                RefreshView: Exibe um controle de atualização (pull-to-refresh).

                WebView: Exibe conteúdo da web.

                MediaElement: Exibe e reproduz mídia (áudio/vídeo).-->
            <StackLayout Grid.Row="1" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center">

                <StackLayout WidthRequest="320" >
                    <StackLayout>
                        <Label Text="Nome completo:" HorizontalOptions="Start"/>
                        <Entry x:Name="NomeCompleto" Keyboard="Text" Placeholder="Digite seu nome completo" Margin="0, 10, 0, 0"/>
                    </StackLayout>
                </StackLayout>

                <StackLayout WidthRequest="320" Margin="0, 20, 0, 0">
                    <Label Text="Data de nascimento" HorizontalOptions="Start"/>
                    <DatePicker x:Name="DataNascimento" Format="dd/mm/yyyy" Margin="0, 10, 0, 0"/>
                </StackLayout>

                <StackLayout WidthRequest="320" Margin="0, 20, 0, 0">
                    <Label Text="Gênero"/>
                    <Picker x:Name="Genero" Margin="0, 10, 0, 0" >
                        <Picker.Items>
                            <x:String>Masculino</x:String>
                            <x:String>Feminino</x:String>
                            <x:String>Prefiro não declarar</x:String>
                        </Picker.Items>
                    </Picker>
                </StackLayout>

                <StackLayout WidthRequest="320" Margin="0, 20, 0, 0">
                    <Label Text="Idade"/>
                    <Entry x:Name="Idade" Keyboard="Numeric" Placeholder="Idade" Margin="0, 10, 0, 0"/>
                </StackLayout>

            </StackLayout>
            <!--Linha 3: botão de envio do formulário-->
            <StackLayout Grid.Row="2" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center">
                <Button x:Name="Cadastrar" Text="Cadastrar"/>
            </StackLayout>
        </Grid>
    </ScrollView>

</ContentPage>
